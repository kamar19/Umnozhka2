<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.firstSet.umnozhka">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="18"
        tools:ignore="ScopedStorage" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/appName"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.firstSet.umnozhka.MakeFotoActivity" />
        <activity android:name="com.firstSet.umnozhka.FinishLeassonActivity" />
        <activity android:name="com.firstSet.umnozhka.GradebookActivity" />
        <activity android:name="com.firstSet.umnozhka.StartActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.firstSet.umnozhka.PrefActivity" />
        <activity android:name="com.firstSet.umnozhka.MainActivity" />
    </application>

</manifest>